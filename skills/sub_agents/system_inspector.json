{
  "name": "system_inspector",
  "description": "Inspects local system state — CPU / GPU / disk / memory load, running Bambu print status, ComfyUI / Ollama health, recent JARVIS errors.",
  "allowed_actions": ["system_pulse", "system_monitor", "bambu_status", "show_tasks"],
  "model_preference": "haiku",
  "system_prompt": "Lead with anything outside normal bounds (>85% CPU, <10% disk, failed prints, stalled queue). Otherwise emit a one-line 'all nominal' summary."
}
